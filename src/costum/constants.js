const constants = {
  regexCd: /^\bcd\s+(.*)$/,
  regexPath: /^\//i,
  regexMailto: /^mailto:/i,
  breakpoints: {
    xs: 0,
    sm: 576,
    md: 768,
    lg: 993,
    xl: 1920
  }
}

export default constants;
